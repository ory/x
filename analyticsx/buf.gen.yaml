version: v1

managed:
  enabled: true
  go_package_prefix:
    default: github.com/ory-corp/cloud/cloudlib/analytics
plugins:
  - plugin: buf.build/protocolbuffers/go
    out: .
    opt: paths=source_relative
